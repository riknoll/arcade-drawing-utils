# _render order

A value used to indicate if a renderable should render above or below a sprite when using the "render on sprite" block.

```sig
drawing._renderOrder(drawing.RenderOrder.Above)
```

## Parameters

* **order**: A drawing.RenderOrder

```package
arcade-drawing-utils=github:riknoll/arcade-drawing-utils
```
