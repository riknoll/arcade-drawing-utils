# remove renderables

Removes any renderables registered on a sprite.

```sig
drawing.removeRenderables(sprites.create(img`.`, SPriteKind.Player))
```

## Parameters

* **sprite**: The sprite to remove registered renderables from

```package
arcade-drawing-utils=github:riknoll/arcade-drawing-utils
```
