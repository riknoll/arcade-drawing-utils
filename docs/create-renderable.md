# create renderable

Creates a renderable that will be drawn at the specified z index. By default, renderables don't anything until they have their render function set

```sig
let myRenderable = drawing.createRenderable(0)
```

## Parameters

* **z**: The z to run this renderable's render function at

```package
arcade-drawing-utils=github:riknoll/arcade-drawing-utils
```
