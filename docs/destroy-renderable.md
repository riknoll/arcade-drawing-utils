# destroy renderable

Destroys a renderable. After calling this, the renderable will no longer draw to the screen.

```sig
drawing.destroyRenderable(drawing.createRenderable(0))
```

## Parameters

* **renderable**: The renderable to destroy

```package
arcade-drawing-utils=github:riknoll/arcade-drawing-utils
```
